/**
 * Frozen v2.0 "Now" media panel.
 * Snapshot from live RSS feeds — re-run archive:snapshot to refresh if desired.
 * External poster URLs may expire; prefer local copies under /archive/v2/media/ when added.
 */

export const frozenMediaData = {
  film: null,
  book: null,
  anime: null,
};
